spring.application.name=expense-tracker

# ===============================
# DATABASE CONFIGURATION (H2 local development)
# ===============================
spring.datasource.url=jdbc:h2:mem:expense_tracker_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA / Hibernate Properties
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Initial Schema file
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# PostgreSQL example configuration (uncomment when running against PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/expense_tracker_db
# spring.datasource.username=postgres
# spring.datasource.password=password
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
